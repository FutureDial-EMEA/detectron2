name: Build & push detectron2 base image

on:
  release:
    types: [published]
  workflow_run:
    workflows: ["Build CPU Wheel"]
    branches: [main]
    types: [completed]
  workflow_dispatch:
    inputs:
      wheel-run-id:
        description: "Run ID of the 'Build CPU Wheel' workflow that produced the artifact"
        required: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: futuredial-emea/detectron2-base

jobs:
  build-push:
    runs-on: ubuntu-latest
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Extract version from release tag
        id: version
        run: echo "version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

      - name: Resolve wheel run ID
        id: wheel-run
        run: |
          if [ -n "${{ inputs.wheel-run-id }}" ]; then
            echo "id=${{ inputs.wheel-run-id }}" >> "$GITHUB_OUTPUT"
          else
            echo "id=${{ github.event.workflow_run.id }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Download detectron2 CPU wheel
        uses: actions/download-artifact@v4
        with:
          name: detectron2-cpu-wheel
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ steps.wheel-run.outputs.id }}
          path: dist/

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune -af
          df -h

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.version.outputs.version }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .github/workflows/Dockerfile.base
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
